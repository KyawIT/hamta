import { TestBed } from '@angular/core/testing';
import { AboutComponent } from './about.component';

describe('AboutComponent', () => {
  it('creates', () => {
    expect(TestBed.createComponent(AboutComponent).componentInstance).toBeTruthy();
  });
});
