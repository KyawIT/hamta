import { TestBed } from '@angular/core/testing';
import { LocationComponent } from './location.component';

describe('LocationComponent', () => {
  it('creates', () => {
    expect(TestBed.createComponent(LocationComponent).componentInstance).toBeTruthy();
  });
});
