import { TestBed } from '@angular/core/testing';
import { SocialComponent } from './social.component';

describe('SocialComponent', () => {
  it('creates', () => {
    expect(TestBed.createComponent(SocialComponent).componentInstance).toBeTruthy();
  });
});
