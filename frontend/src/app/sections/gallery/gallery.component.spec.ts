import { TestBed } from '@angular/core/testing';
import { GalleryComponent } from './gallery.component';

describe('GalleryComponent', () => {
  it('creates', () => {
    expect(TestBed.createComponent(GalleryComponent).componentInstance).toBeTruthy();
  });
});
